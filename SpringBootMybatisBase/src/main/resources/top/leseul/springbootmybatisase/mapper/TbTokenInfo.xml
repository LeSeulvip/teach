<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- TbToken mapper -->
<mapper namespace="top.leseul.springbootmybatisbase.dao.TbTokenInfoDAO">

  <!-- 删除所有过期tokenInfo -->
  <delete id="deleteTbTokenInfos">
    delete from TbTokenInfo
    where token in
    (
    select token from TbToken
    where timestampdiff (minute,lastupdate,now())
    &gt;
    (
    select configValue from TbConfig
    where configKey='token.timeout'
    )
    )
  </delete>
</mapper>